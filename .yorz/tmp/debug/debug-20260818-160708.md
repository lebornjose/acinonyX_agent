---
status: resolved
active:
updated_at: '2026-08-18 16:07:08'
---

## Debug 1 · 管理端 API 代理连接被拒绝

- 状态：resolved
- 快照：无法创建，当前 Git index 不可写；基线 HEAD 为 `585ecdf57d7d38e365d97db21ef4bcf841cae967`
- 进入时间：'2026-08-18 16:05:00'

### 1.1 Bug 现象与复现

管理端 Vite 运行在 5174，请求 `/api/conversations` 和 `/api/models` 时出现 `ECONNREFUSED`。直接请求 `http://127.0.0.1:4001/api/health` 同样无法连接。

### 1.2 关联链路分析

Vite 管理端将 `/api` 代理到 `http://localhost:4001`。4001 由 `apps/api` 提供，API 启动前必须连接并初始化 MySQL。

### 1.3 Debug 基线

工作区已有未提交改动，`git stash create` 因 Git index 不可写失败；未修改业务代码。

### 1.4 假设看板

- API 进程未启动：成立。4001 无监听，健康检查连接失败。
- API 启动后因数据库连接失败退出：成立。手动执行 `pnpm dev:api` 后进程退出并输出数据库连接错误。
- Vite 代理目标端口配置错误：排除。`apps/admin/vite.config.js` 目标为预期的 4001。

### 1.5 证据

执行 `pnpm dev:api` 的结果显示：环境配置被读取为 `110.42.146.37:3306/110.42.146.37`，随后报 `connect EPERM 110.42.146.37:3306`，API 未进入监听状态。

### 1.6 脚手架清单

- 无临时代码、Mock、调试日志或业务文件修改。

### 1.7 收尾核对

- 根因：业务 API 因 MySQL 连接失败退出，导致前端代理连接被拒绝。
- 修复动作：需要修正本地 `.env` 中的 MySQL 连接配置，并确保数据库可访问后重新启动 API；本次未修改密钥或环境文件。
- 验证：当前代码未改动；API 启动验证已明确失败原因。
