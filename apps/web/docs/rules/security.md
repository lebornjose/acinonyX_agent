# 安全与可访问性规则

- 禁止使用 `v-html`，除非内容经过可靠的 HTML 清理；用户输入展示时必须保持文本转义。
- Token、密码和敏感信息不得写入源码、日志或 localStorage。
- 交互元素优先使用语义化 HTML。
- 按钮必须有明确文本或 `aria-label`，图片必须提供 `alt`。
- 颜色不能作为传达状态的唯一方式。
- 弹窗、下拉菜单和表单需要支持键盘操作。
- 危险操作必须二次确认。
