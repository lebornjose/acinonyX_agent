# 交付与验证规则

- `src/` 下任何 `.js`、`.vue`、`.ts`、`.tsx` 文件不得超过 500 行。
- 新增公共方法前必须搜索现有 `src/utils/`，禁止创建重复方法。
- 公共方法必须从 `src/utils/index.js` 统一导出。
- 新增业务逻辑应补充至少一个测试；修复 bug 必须增加回归测试。
- 交付前检查文件行数、重复方法和组件、`v-html`、模板内请求、未处理的异步状态和不稳定的列表 key。
- 执行 `pnpm --filter web-client build`。
