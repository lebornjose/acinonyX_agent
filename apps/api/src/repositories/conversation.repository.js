import { pool } from "../database/mysql.js";
import { nowUnixSeconds } from "../utils/time.js";
export async function listByUser(userId) { const [rows] = await pool.query("SELECT id, user_id AS userId, title, created_at AS createdAt, updated_at AS updatedAt FROM conversations WHERE user_id = ? ORDER BY updated_at DESC", [userId]); return rows; }
export async function createConversation(id, userId, title) { const timestamp = nowUnixSeconds(); await pool.query("INSERT INTO conversations (id,user_id,title,created_at,updated_at) VALUES (?,?,?,?,?)", [id, userId, title, timestamp, timestamp]); return findById(id, userId); }
export async function findById(id, userId) { const [rows] = await pool.query("SELECT id, user_id AS userId, title, created_at AS createdAt, updated_at AS updatedAt FROM conversations WHERE id=? AND user_id=?", [id, userId]); return rows[0]; }
export async function listMessages(id) { const [rows] = await pool.query("SELECT id, role, content, created_at AS createdAt FROM messages WHERE conversation_id=? ORDER BY created_at ASC", [id]); return rows; }
export async function addMessages(conversationId, userMessage, assistantMessage) { const timestamp = nowUnixSeconds(); await pool.query("INSERT INTO messages (id,conversation_id,role,content,created_at) VALUES (?,?,?,?,?),(?,?,?,?,?)", [userMessage.id, conversationId, "user", userMessage.content, timestamp, assistantMessage.id, conversationId, "assistant", assistantMessage.content, timestamp]); await pool.query("UPDATE conversations SET updated_at=? WHERE id=?", [nowUnixSeconds(), conversationId]); }
export async function listAll() { const [rows] = await pool.query("SELECT id, user_id AS userId, title, created_at AS createdAt, updated_at AS updatedAt FROM conversations ORDER BY updated_at DESC"); return rows; }
export async function deleteConversation(id, userId) { const [result] = await pool.query("DELETE FROM conversations WHERE id = ? AND user_id = ?", [id, userId]); return result.affectedRows > 0; }
